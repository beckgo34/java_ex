package memo;

public class String {
    /**
     * 자바의 기본형(primitive) 타입, 참조형(reference)타입
     *
     * * 기본형 타입
     *      -종류 : int. double, float, byte, char 등
     *      -변수에 선언한 값을 실제 가지고 있음.
     * * 참조형 타입
     *      -종류: String, 직접 선언한 Class 타입
     *        ``Wrapper Class 타입:Integer, Doulbe, Boolean, Long 등
     *      -선언한 변수(객체 쪼는 인스턴스라고 부름)에는 해당 값을 저장한 주소값을 가지고 있음.
     *      -주소값을 가지고 실제 값을 찾아간다고 해서 참조형 타입
     * * 값이 같은지 비교
     *      - 기본형타입
     *       ` ==를 사용
     *      - 참조형타입
     *       ` ==을 쓰지 않고 equals() 라는 메서드를 사용해야 함
     *       `  equals()의 리턴타입(반환타입)은 boolean
     *       ` 예)
     *              //참조형 변수 a와 b의 값이 같은지 보려면
     *              a.equals(b)
     */
}
